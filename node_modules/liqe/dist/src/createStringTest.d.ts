import type { LiqeQuery } from './types';
declare type RegExpCache = Record<string, RegExp>;
export declare const createStringTest: (regexCache: RegExpCache, ast: LiqeQuery) => (subject: string) => string | false;
export {};
